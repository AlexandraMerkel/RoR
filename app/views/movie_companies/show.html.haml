%p#notice= notice

%p
  %b 
    = MovieCompany.human_attribute_name(:name) + ":"
  = @movie_company.name

= link_to t('helpers.edit'), edit_movie_company_path(@movie_company)
\|
= link_to t('helpers.back'), movie_companies_path
