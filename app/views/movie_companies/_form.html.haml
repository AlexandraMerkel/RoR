= form_for @movie_company do |f|
  - if @movie_company.errors.any?
    #error_explanation
      %h2= "#{pluralize(@movie_company.errors.count, "error")} prohibited this movie_company from being saved:"
      %ul
        - @movie_company.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit
