%h1 Listing prices

%table
  %thead
    %tr
      %th Cost
      %th Cinema timetable
      %th Zone
      %th
      %th
      %th

  %tbody
    - @prices.each do |price|
      %tr
        %td= price.cost
        %td= price.cinema_timetable
        %td= price.zone
        %td= link_to 'Show', price
        %td= link_to 'Edit', edit_price_path(price)
        %td= link_to 'Destroy', price, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Price', new_price_path
