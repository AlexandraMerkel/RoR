%h1 
  =t('helpers.listing')
  \:
  =Price.model_name.human(count: 2)

%table
  %thead
    %tr
      %th= Price.human_attribute_name(:cost)
      %th
      %th
      %th= Price.human_attribute_name(:cinema_timetable)
      %th
      %th
      %th
      %th= Price.human_attribute_name(:zone)

  %tbody
    - @prices.each do |price|
      %tr
        %td= price.cost
        %td= price.cinema_timetable.cinema.name
        %td= price.cinema_timetable.movie.name
        %td= price.cinema_timetable.hall
        %td= price.cinema_timetable.start 
        %td
        %td
        %td= price.zone.name 
        
        %td= link_to t('helpers.show'), price
        %td= link_to t('helpers.edit'), edit_price_path(price)
        %td= link_to t('helpers.destroy'), price, :method => :delete, :data => { :confirm => t('helpers.sure')  }

%br

= link_to t('helpers.new') + ' ' + Price.model_name.human(count: 1), new_price_path
